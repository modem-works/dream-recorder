# --- Secret Keys ---

# Obtain from OpenAI dashboard
OPENAI_API_KEY="your-openai-api-key-here"

# Obtain from Luma Labs dashboard
LUMALABS_API_KEY="your-luma-labs-api-key-here"

# --- Optional Overrides for config.py Defaults ---

# --- Logging ---
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL="INFO"

# --- Flask Settings ---
# FLASK_ENV="development"  # Set to development for debug mode
# HOST="0.0.0.0"           # Host to bind the server to
# PORT=5000                # Port to run the server on

# --- Audio Settings ---
# AUDIO_CHANNELS=1
# AUDIO_SAMPLE_WIDTH=2
# AUDIO_FRAME_RATE=44100
# RECORDINGS_DIR="recordings"

# --- OpenAI Settings ---
# WHISPER_MODEL="whisper-1"
# GPT_MODEL="gpt-4o-mini"
# GPT_SYSTEM_PROMPT="Your custom system prompt here..."
# GPT_TEMPERATURE=0.7
# GPT_MAX_TOKENS=200

# --- Luma Labs Settings ---
# LUMA_API_URL="https://api.lumalabs.ai/dream-machine/v1"
# LUMA_MODEL="ray-2"
# LUMA_RESOLUTION="540p"
# LUMA_DURATION="5s"
# LUMA_ASPECT_RATIO="21:9"
# LUMA_POLL_INTERVAL=5
# LUMA_MAX_POLL_ATTEMPTS=60
# VIDEOS_DIR="videos"

# --- GPIO Service Settings (if different from config.py defaults) ---
# GPIO_PIN=4
# GPIO_FLASK_URL="http://localhost:5000" # Ensure port matches PORT above if changed
# GPIO_SINGLE_TAP_ENDPOINT="/api/wake_device"
# GPIO_DOUBLE_TAP_ENDPOINT="/api/show_previous_dream"
# GPIO_LONG_PRESS_ENDPOINT="/api/trigger_recording"
# GPIO_LONG_PRESS_RELEASE_ENDPOINT="/api/stop_recording"
# GPIO_SINGLE_TAP_MAX_DURATION=0.5
# GPIO_DOUBLE_TAP_MAX_INTERVAL=0.7
# GPIO_LONG_PRESS_MIN_DURATION=1.5
# GPIO_DEBOUNCE_TIME=0.05
# GPIO_STARTUP_DELAY=2
# GPIO_SAMPLING_RATE=0.01 